{
  "services": [
    {
      "name": "docker-db",
      "exec": [
        "/usr/bin/docker", "run",
        "-i",
        "--rm",
        "--name={name}",
        "--network={networkname}",
        "{image_name}"
      ],
      "execStop": [ "/usr/bin/docker", "stop", "{name}" ],
      "context": {
        "name": "{service_name}_{engine_name}",
        "image_name": "health_{engine_name}"
      },
      "abstract": true,
      "type": "db"
    },
    {
      "name": "mongodb",
      "type": "db",
      "extends": "docker-db",
      "retry": 2,
      "context": {
        "engine_name": "mongodb"
      }
    },
    {
      "name": "rabbitmq",
      "exec" : [ "/usr/bin/docker", "run",
        "-i",
        "--rm",
        "--name=rabbitmq",
        "--network={networkname}",
        "rabbitmq:3-management"],
      "execStop": [ "/usr/bin/docker", "stop", "rabbitmq"],
      "type": "infra",
      "extraData": [
        {
          "name": "rabbitmq management",
          "exec": ["/bin/bash", "-c",
            "sleep 1 && echo \"http://$(docker inspect rabbitmq | jq -r '.[].NetworkSettings.Networks.\"{networkname}\".IPAddress'):15672/\""
          ],
          "type": "link"
        },
        {
          "name": "nameko cmdline",
          "exec": ["/bin/bash", "-c",
            "sleep 1 && echo \"nameko shell --broker amqp://guest:guest@$(docker inspect rabbitmq | jq -r '.[].NetworkSettings.Networks.\"{networkname}\".IPAddress')\""
          ],
          "type": "string"
        }
      ]
    },
    {
      "name": "docker",
      "exec" : ["/usr/bin/docker", "run",
        "--rm", "-i",
        "-v", "{root}/services/maiev-base/app/common/:/app/common:ro",
        "-v", "{root}/services/maiev-base/run_dev.sh:/run_dev.sh:ro",
        "-v", "{current_dir}/app/service:/app/service:ro",
        "-v", "{current_dir}/app/scale.json:/app/scale.json:ro",
        "{extra_volume}",
        "-e", "SERVICE_NAME={service_name}",
        "-e FORCE_COLOR=true",
        "--network={networkname}",
        "--link", "rabbitmq:rabbitmq",
        "--link", "maiev_mongodb:maiev_mongodb",
        "{extra_link}",
        "--name={docker_name}",
        "{service_name}",
        "/run_dev.sh"],
      "execStop": ["/usr/bin/docker", "stop", "{docker_name}"],
      "require": [
        "rabbitmq"
      ],
      "abstract": true,
      "type": "Wr"
    }
  ],
  "context": {
    "service_name": "{dirname}",
    "docker_name": "{service_name}",
    "networkname": "maiev-dev",
    "extra_link": "",
    "extra_volume": ""
  },
  "discover": [
    "services",
    "test"
  ]
}